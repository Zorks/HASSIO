toggle_front_door:
  alias: 'Toggle Front Door'
  sequence:
    - service_template: >
        {% if is_state('lock.schlage_lock_locked', 'locked') %}
          lock.unlock
        {% else %}
          lock.lock
        {% endif %}
      data:
        entity_id: lock.schlage_lock_locked