relock_front_door:
  alias: 'Relock Front Door'
  sequence:
    - service: lock.lock
      data_template:
        entity_id: >
          {% if is_state("lock.lock_front_door_lock", "unlocked") %}
          lock.lock_front_door_lock
          {% endif %}